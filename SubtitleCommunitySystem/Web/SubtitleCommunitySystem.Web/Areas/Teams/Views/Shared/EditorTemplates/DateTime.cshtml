@model DateTime?

@(Html.Kendo().DateTimePickerFor(m => m)
        .Culture("en-GB")
        //.Format("dd/MM/yyyy")
        //    .ParseFormats(new[]
        //{
        //    "dd/MM/yyyy"
        //})
    )