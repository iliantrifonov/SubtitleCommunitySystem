@model DateTime?

@(Html.Kendo().DateTimePickerFor(m => m)
        .Culture("de-DE")
        //.Format("dd/MM/yyyy")
        //    .ParseFormats(new[]
        //{
        //    "dd/MM/yyyy"
        //})
    )